CanvasExample10:

    Button:
        pos: self.width, root.height - self.height
        size: dp(100), dp(100)
        text: "Gorizontal"
        on_press: root.move_horizontal()
    Button:
        pos: self.width*2+20, root.height - self.height
        size: dp(100), dp(100)
        text: "Vertikal"
        on_press: root.move_vertikal()
